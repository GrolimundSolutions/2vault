version: "3.7"
services:

  # Backend
  2vault-backend:
    build: .
    ports:
      - "8080:8080"
    networks:
      - vaultnet

  # Frontend
  2vault-frontend:
    build: ./deployments/docker/frontend
    ports:
      - "8081:80"
    networks:
      - vaultnet

networks:
  vaultnet:


